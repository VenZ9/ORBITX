<?xml version="1.0" encoding="utf-8"?>
<!-- ORBIT — primary (Save) action. The ONE soft, filled, high-contrast object
     on a screen: pill radius, solid ember, white label. Nothing else in the
     interface competes with it. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#33FFFFFF">
    <item>
        <selector>
            <item android:state_enabled="false">
                <shape android:shape="rectangle">
                    <corners android:radius="999dp" />
                    <solid android:color="@color/ox_surface_raised" />
                    <stroke android:width="1dp" android:color="@color/ox_hairline" />
                </shape>
            </item>
            <item android:state_pressed="true">
                <shape android:shape="rectangle">
                    <corners android:radius="999dp" />
                    <solid android:color="@color/ox_ember_deep" />
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="999dp" />
                    <solid android:color="@color/ox_ember" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
