<?xml version="1.0" encoding="utf-8"?>
<!-- ORBIT Desktop — the rank badges for occupants 4 and beyond: a plain rim, so
     the three that matter are the three that are coloured. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/od_window" />
    <stroke
        android:width="@dimen/od_stroke_rule"
        android:color="@color/od_rule_strong" />
</shape>
