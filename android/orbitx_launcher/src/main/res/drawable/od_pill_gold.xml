<?xml version="1.0" encoding="utf-8"?>
<!-- ORBIT Desktop — GOLD status pill. Reserved for the Java runtime. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/od_pill_gold_wash" />
    <stroke
        android:width="@dimen/od_stroke_rule"
        android:color="@color/od_pill_gold" />
    <corners android:radius="@dimen/od_radius_pill" />
</shape>
